# seafile.conf - Seafile Community Edition 13
# No Pro options (e.g. fs_id_list_max_threads). No virus scan.

[fileserver]
port = 8082

# Number of worker threads for HTTP requests. Default: 10.
worker_threads = 6

# Go fileserver: in-memory fs cache size in MB. Default: 2000.
fs_cache_limit = 500
